# XiaoXu Skills

Each child directory is one Agent Skill. Startup scans only `name` and
`description`; the full `SKILL.md` is loaded after activation. Optional
`references/`, `scripts/`, and `assets/` remain inside the skill boundary.
